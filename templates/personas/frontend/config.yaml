name: Frontend Engineer
role: engineer
label: frontend
escalates_to: ceo

required_tools:
  - gh                             # GitHub CLI – issue management
  # Add repo-specific tools below:
  # - mcp__plugin_playwright_playwright  # Browser automation

runtime:
  model: sonnet                    # Component work — moderate complexity
  thinking_effort: medium
  max_turns: 200
  enable_chrome: false
  timeout: 0
  extra_args: []
