name: Orchestrator
role: orchestrator
label: null
is_default: true
escalates_to: ceo

required_tools:
  - gh                             # GitHub CLI – verified via `gh auth status`

runtime:
  model: haiku                     # Routing is mechanical – cheapest model works
  thinking_effort: low
  max_turns: 50
  enable_chrome: false
  timeout: 0
  extra_args: []
