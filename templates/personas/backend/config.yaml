name: Backend Engineer
role: engineer
label: backend
escalates_to: ceo

required_tools:
  - gh                             # GitHub CLI – issue management
  # Add repo-specific tools below:
  # - mcp__neon                    # Neon Postgres

runtime:
  model: opus                      # Deep implementation needs strong reasoning
  thinking_effort: high
  max_turns: 300
  enable_chrome: false
  timeout: 0
  extra_args: []
